#code
from collections import Counter
for _ in range(int(input())):
    n=int(input())
    l=list(map(int,input().split()))
    d=Counter(l)
    res=0
    for i in d.values():
        res+=i//2
    print(res*2)
